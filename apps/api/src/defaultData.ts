import { starterSiteData, type SiteData } from "@company/contracts";

/**
 * The API seed is intentionally neutral. Replace the shared starter content
 * in packages/contracts/src/starterSiteData.ts before publishing a project.
 */
export const defaultData: SiteData = structuredClone(starterSiteData);
